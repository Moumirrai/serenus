import { defineStore } from "pinia";
import { State } from "@/store/player/types";


export const usePlayerStore = defineStore("auth", {
  state: (): State => {
    return {  };
  },
  getters: {
    
  },
  actions: {
  },
});
